How to test:
1. Upload the notebook to Google drive
2. Open the notebook via. Google colab
3. Under files, upload all the image (training, valid, test) sets
  i) Upload all the model files contained in my submission as well:
    	cls_model.dat.data-00000-of-00001
	cls_model.dat.index
	cls_model.dat.meta
    	bb_model.dat.data-00000-of-00001
	bb_model.dat.index
	bb_model.dat.meta
5. In the main() function, ensure that TRAIN is set to False
6. Ensure that GPU is being used, and then click run all under runtime

If you would like to train: 
1. Set the TRAIN variable in the main() function to True
2. Run all
	






